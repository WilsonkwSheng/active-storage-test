<div>
  <h1>All Users Page</h1>
  <% @users.each do |user| %>
    <div>
      <% if user.profile_picture.present? %>
        <%= link_to(image_tag(user.profile_picture.variant(resize_to_limit: [150, 150])), rails_blob_path(user.profile_picture, disposition: "preview")) %>
      <% else %>
        <%= image_tag user.profile_picture %>
      <% end %>
    </div>
    <% if current_user == user %>
      <div>
        <%= link_to "Edit Profile", edit_user_path(user) %>
      </div>
    <% end %>
  <% end %>
</div>
